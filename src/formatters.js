// Functions that take data resulting from db query, format them and write to console.

const formatDepartmentData = (allDepartmentsData) => {
    console.log('dddddddddddddddd', allDepartmentsData)
};


module.exports = {
    formatDepartmentData: formatDepartmentData
}
